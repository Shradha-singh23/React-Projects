import React from "react";
import Button from "./Button"

export default function App(){
    return(
        <div>
            <Button bgcolor="secondary" isRoundedCorners={false}> Click Me! </Button>
        </div>
    )
}